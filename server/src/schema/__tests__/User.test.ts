describe("User Resolvers", () => {
  describe("Queries", () => {
    describe("User.user", () => {
      it("returns a user", () => {
        //
      })
    })

    describe("User.users", () => {
      it("returns a list of users", () => {
        //
      })
    })
  })

  describe("Mutations", () => {
    describe("User.createUser", () => {
      it("creates a new user and returns result", () => {
        //
      })
    })
  })
})
