describe("User Resource", () => {
  it("initializes a User model", () => {})

  describe("REST endpoints", () => {
    describe("/users", () => {
      it("GET returns a list of users", () => {})

      it("POST creates a new user and returns result", () => {})
    })

    describe("/user/:id", () => {
      it("GET returns a user", () => {
        //
      })
    })
  })
})
