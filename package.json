{
  "name": "graphql-by-example",
  "version": "1.0.0",
  "repository": "git@github.com:damassi/graphql-by-example.git",
  "author": "Christopher Pappas <damassi.pappas@gmail.com>",
  "license": "MIT",
  "scripts": {
    "setup": "yarn install && cd server && yarn install && cd ../client && yarn install",
    "start": "concurrently --kill-others 'cd server && yarn start' 'cd client && yarn start'"
  },
  "devDependencies": {
    "concurrently": "^5.0.2"
  }
}
